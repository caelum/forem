<div class="crayons-card crayons-card--secondary p-4">
  <h2 class="fs-l fw-bold mb-2">
    <a href="<%= about_path %>"><%= community_name %></a> é uma comunidade de <%= number_with_delimiter User.registered.estimated_count %> surpreendentes <%= community_members_label %>
  </h2>
  <p class="color-base-70 mb-4">
    <% if SiteConfig.tagline.present? %>
      <%= SiteConfig.tagline %>
    <% else %>
      Faça login para participar e personalizar sua experiência.
    <% end %>
  </p>
  <div>
    <a href="<%= sign_up_path(state: "new-user") %>" class="crayons-btn w-100 mb-2" aria-label="Create new account">Criar nova conta</a>
    <a href="<%= sign_up_path %>" class="crayons-btn crayons-btn--ghost-brand w-100" aria-label="Log in">Entrar</a>
  </div>
</div>
